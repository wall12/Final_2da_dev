<h1> Editar Marca </h1>
<%= form_with scope: @item, url: marcas_update_path, method: :PUT do |f| %>    
    <%= f.hidden_field  :id %>   
    <p>
    <label>Nombre Marca</label> <br>
    <%= f.text_field :nombre, class: "form-control" %>
    </p>
    <div class="alert alert-warning" role="alert">
    <p>Si el item introducido no envia, significa que ya existe un item con el mismo nombre</p>
    </div>
    <%= f.submit "Enviar", disable_with: 'Enviando...',class: "btn btn-primary"  %>
    
   <% end %>
   